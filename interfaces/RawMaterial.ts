export interface IRawMaterialRequest {
  code: string;
  name: string;
  stockQuantity: number;
}

export interface IRawMaterialResponse {
  id: number;
  code: string;
  name: string;
  stockQuantity: number;
}
